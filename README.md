# OakPyTest
A small web app that lets you upload a transactions CSV (or generate a sample) and then flags unusual transactions with a severity score, human-readable reasons, and a couple of lightweight charts. uses Streamlit UI.
